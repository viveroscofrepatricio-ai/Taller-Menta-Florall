// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Cambiá esta URL por la de tu sitio cuando tengas dominio propio
  site: 'https://tallermenta.netlify.app',
  output: 'static',
});
